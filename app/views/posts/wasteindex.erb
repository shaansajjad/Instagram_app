<% @post.each do |x| %>

<%if @post.user.id == current_user.id %>

<div class="white">
<%= image_tag x.image.url(:medium) %>
<br>
<%= x.description %>
<td><%= link_to 'Edit', edit_post_path(x), class: "btn btn-info" %></td>
<td><%= link_to 'Destroy', post_path(x),
              method: :delete,
              data: { confirm: 'Are you sure?' } , class: "btn btn-info"%></td>

	</div>
</td>

<%else%>

<div class="white">
<%= image_tag x.image.url(:medium) %>
<br>
<%= x.description %>
</div>

<% end %>
<% end %>


////////////////////////////////////////////////////







<div class="posts-wrapper row">
  <% @post.each do |post| %>
    <div class="post">
      <div class="post-head">
<!--         <div class="thumb-img"></div>
 -->        <!-- <div class="user-name">
          <%= post.user.user_name %>
        </div> -->
      <!-- </div> -->
      <div class="image center-block">
        <%= link_to (image_tag post.image.url(:medium), class:'img-responsive'), post_path(post) %>
      </div>
      <div class="post-bottom">
        <div class="caption">
          <!-- <div class="user-name">
            <%= post.user.user_name %>
          </div> -->
          <%= post.caption %>
        </div>
        <% if post.comments %>
          <% post.comments.each do |comment| %>
            <div class="comment">
              <div class="user-name">
                <%= comment.user.user_name %>
              </div>
              <div class="comment-content">
                <%= comment.content %>
              </div>
              <% if comment.user == current_user %>
                <%= link_to post_comment_path(post, comment), method: :delete, data: { confirm: "Are you sure?" } %>
              <% end %>
            </div>
          <% end %>
        <% end %>
        <div class="comment-form">
          <%= form_for [post, post.comments.new] do |f| %>
            <%= f.text_field :content, placeholder: 'Add a comment...' %>
          <% end %>
        </div>
      </div>
    </div>
  <% end %>
</div>










////////////// finaalalllllll////////////////////



<% @posts.each do |x| %>

<%= x.user.first_name if x.user %>
<% if x.user_id == current_user.id %>



<div class="white">
<%= image_tag x.image.url(:medium) %>
<br>
<%= x.description %>
<td><%= link_to 'Edit', edit_post_path(x), class: "btn btn-info" %></td>
<td><%= link_to 'Destroy', post_path(x),
              method: :delete,
              data: { confirm: 'Are you sure?' } , class: "btn btn-info"%></td>

  </div>
</td>

<%else%>

<div class="white">
<%= image_tag x.image.url(:medium) %>
<br>
<%= x.description %>
</div>

<% end %>

<% end %>

